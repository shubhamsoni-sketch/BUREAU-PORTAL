import AppLayout from '@/crm/components/AppLayout';
import LeadManagementContent from './components/LeadManagementContent';

export default function LeadManagementPage() {
  return (
    <AppLayout>
      <LeadManagementContent />
    </AppLayout>
  );
}
