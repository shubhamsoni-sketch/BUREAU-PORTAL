import AppLayout from '@/crm/components/AppLayout';
import LenderSelectionContent from './components/LenderSelectionContent';

export default function LenderSelectionPage() {
  return (
    <AppLayout>
      <LenderSelectionContent />
    </AppLayout>
  );
}
